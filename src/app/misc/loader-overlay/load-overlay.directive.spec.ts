import { LoadOverlayDirective } from './load-overlay.directive';

describe('LoadOverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
